// Export links to backend and similar

let dict = {
    "backend": "http://localhost:8000",
    "frontend": "http://localhost:3000",
}

let backend = dict["backend"];
let frontend = dict["frontend"];

export { backend, frontend };